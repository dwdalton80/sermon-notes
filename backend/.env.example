# Copy to backend/.env and fill in. backend/.env is gitignored.

# --- Speech to text -------------------------------------------------------
# "deepgram" to use the real streaming STT; anything else uses the mock fixture.
STT_PROVIDER=deepgram
DEEPGRAM_API_KEY=

# --- Summarization ------------------------------------------------------
# "claude" to use the real summarizer; anything else uses the mock fixture.
SUMMARIZER_PROVIDER=claude
ANTHROPIC_API_KEY=
# Model for the ~25s summarize loop. Default claude-opus-5. For lower cost per
# sermon, try claude-sonnet-5 or claude-haiku-4-5.
SUMMARIZER_MODEL=claude-opus-5

# --- Server ------------------------------------------------------------
PORT=8787
# Fixture used when a provider is set to mock.
FIXTURE=acts2
